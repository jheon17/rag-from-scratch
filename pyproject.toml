[project]
name = "rag-basic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pypdf>=6.16.2",
    "sentence-transformers>=6.0.1",
]

[project.scripts]
rag-basic = "rag_basic:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
