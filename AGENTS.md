# rag-basic Project Instructions

## 프로젝트 목적

이 프로젝트의 최우선 목적은 완성된 RAG 애플리케이션을 빠르게 만드는 것이 아니라, 사용자가 RAG의 기본 원리를 직접 이해하면서 단계별로 구현하는 것이다.

사용자는 RAG를 처음 학습하고 있으며 Python 초중급 수준이다.

따라서 구현 속도보다 다음을 우선한다.

`개념 이해 → 최소 구현 → 실제 실행 → 결과 확인 → 다음 단계`

## 학습 진행 원칙

새로운 RAG 단계를 구현하기 전에 먼저 다음을 설명한다.

1. 이번 단계에서 무엇을 하는가
2. 왜 이 단계가 필요한가
3. 이전 단계의 결과를 어떻게 입력으로 사용하는가
4. 이번 단계의 출력은 무엇인가
5. 실제 RAG 전체 구조에서 어디에 해당하는가

설명을 마친 뒤 현재 단계에 필요한 최소한의 코드만 구현한다.

사용자가 요청하지 않았다면 여러 RAG 단계를 한 번에 구현하지 않는다.

예를 들어 청킹을 학습하는 단계에서 임베딩, 벡터 데이터베이스, 검색기, LLM 연결까지 한꺼번에 구현하지 않는다.

## RAG 학습 순서

기본 학습 흐름은 다음과 같다.

`PDF 로딩 → 텍스트 확인 → 청킹 → 임베딩 → 벡터 저장 → 검색 → 프롬프트 구성 → LLM 답변 → 검색·답변 품질 평가`

현재 단계가 충분히 이해되고 실제 데이터로 검증된 뒤 다음 단계로 진행한다.

단, 사용자가 순서를 명시적으로 변경하면 사용자 요청을 따른다.

## 라이브러리 사용 원칙

RAG의 기본 동작을 학습하는 초기 단계에서는 가능한 한 핵심 원리가 코드에서 직접 보이도록 구현한다.

사용자가 요청하지 않았다면 처음부터 LangChain이나 LlamaIndex 같은 고수준 RAG 프레임워크로 전체 파이프라인을 감싸지 않는다.

먼저 다음 핵심 개념을 직접 이해하는 것을 우선한다.

* 문서를 어떻게 읽는지
* 텍스트를 왜 나누는지
* 청크 크기와 overlap이 어떤 의미인지
* 텍스트가 어떻게 임베딩되는지
* 벡터 유사도로 무엇을 검색하는지
* 검색된 문맥이 LLM에게 어떻게 전달되는지

핵심 원리를 직접 구현하고 이해한 이후 프레임워크를 사용하는 것은 허용한다.

## 코드 난이도

처음부터 클래스 기반 구조나 복잡한 추상화를 만들지 않는다.

함수와 작은 모듈 중심의 단순한 구조를 우선한다.

코드가 복잡해질 필요가 생겼을 때만 리팩터링을 제안한다.

초보자에게 불필요한 디자인 패턴이나 프레임워크 구조를 도입하지 않는다.

## 설명 방식

새로운 용어가 나오면 먼저 쉬운 비유나 작은 예제로 설명한다.

예를 들어 청킹을 설명할 때 바로 라이브러리 코드부터 제시하지 말고 다음과 같이 사고할 수 있도록 설명한다.

`긴 책 한 권을 통째로 검색하는 대신, 의미 있는 작은 구간으로 나누어 필요한 부분을 찾는다.`

그 다음 실제 코드와 연결한다.

코드 설명에서는 모든 문법을 설명할 필요는 없지만, 사용자가 현재 단계의 핵심 원리를 이해하는 데 필요한 줄은 반드시 설명한다.

## 실험과 숫자

청크 크기, overlap, 검색 결과 개수 `k` 같은 값은 정답처럼 제시하지 않는다.

임의의 값을 사용할 경우 다음을 함께 설명한다.

* 이 값이 무엇을 의미하는지
* 왜 처음 실험값으로 선택했는지
* 값을 너무 크게 또는 작게 하면 어떤 문제가 생길 수 있는지
* 이후 어떤 기준으로 조정할 수 있는지

라이브러리의 기본값을 사용하는 경우에도 중요한 기본값이라면 그 사실을 알린다.

## 검증

각 단계는 가능한 한 실제 프로젝트 데이터로 검증한다.

단순히 오류 없이 실행된 것을 성공이라고 판단하지 않는다.

예를 들어 청킹이라면 최소한 다음을 확인한다.

* 청크가 실제로 생성되었는가
* 청크 개수는 합리적인가
* 너무 짧거나 비어 있는 청크가 없는가
* 문장이나 내용이 지나치게 부자연스럽게 잘리지 않았는가
* 원본 문서의 중요한 정보가 사라지지 않았는가

검증 결과를 사용자가 이해할 수 있는 말로 설명한다.

## 기존 환경 보호

현재 프로젝트의 `pyproject.toml`, `uv.lock`, `.venv` 및 기존 코드 구조를 우선 사용한다.

이미 구성된 Python 환경을 새로 만들지 않는다.

새 패키지가 필요한 경우 현재 프로젝트에서 정말 필요한지 먼저 판단하고 기존 `uv` 환경에 맞게 추가한다.

기존에 정상 동작하는 PDF 로딩 코드를 불필요하게 다시 작성하거나 리팩터링하지 않는다.

## 작업 범위

사용자가 한 단계만 요청했다면 해당 단계까지만 완료한다.

다음 단계가 무엇인지는 짧게 설명할 수 있지만 사용자의 요청 없이 구현까지 진행하지 않는다.

작업이 끝났을 때는 사용자가 다음 단계로 넘어가기 전에 현재 단계에서 무엇을 이해해야 하는지 짧게 정리한다.
